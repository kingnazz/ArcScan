{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability set for the ArcScan main window: core IPC and events, the save dialog for exports, and the updater with its restart. External URLs, launchers and file writes go through dedicated Rust commands that validate their input, so no shell, filesystem or opener permission is granted to the webview.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-save",
    "updater:default",
    "process:allow-restart"
  ]
}
